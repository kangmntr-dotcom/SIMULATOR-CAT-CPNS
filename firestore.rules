rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Simulator SKD CPNS Security Rules
    // Allow read/write for application collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
